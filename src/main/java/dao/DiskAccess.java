package dao;

import java.util.Map;
import java.util.logging.Level;
import java.util.logging.Logger;

public class DiskAccess {
    /**
     * Puts key value on to the disk
     * @param key
     * @param value
     */
    public void put(String key, Object value) {
    }

    /**
     * Removes the entry from the disk if available and returns the keyvalue or returns null
     * @param key
     * @return they KeyValue pair
     */
    public KeyValue remove(String key) {
        return null;
    }

    public void clear() {
    }
}
